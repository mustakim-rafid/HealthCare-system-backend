export interface ISchedulePayload {
    startDate: string;
    endDate: string;
    startTime: string;
    endTime: string
}

export interface IScheduleFilterQueries {
    startDateTime: string;
    endDateTime: string;
}